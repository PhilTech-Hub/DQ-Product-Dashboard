// Spinner component to show a loading spinner with a spinning animation

export default function Spinner() {
  return (
    // Spinner container with utility classes from Tailwind CSS
    <div className="animate-spin rounded-full h-12 w-12 border-t-4 border-blue-500 border-solid" />
  );
}
